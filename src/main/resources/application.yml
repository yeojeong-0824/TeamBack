server:
  port: 8888

spring:
  profiles:
    active: local
  redis:
    host: localhost
    port: 6379

JWTKey: DevKey
EmailPassword: ${emailPassword}

autocomplete:
  limit: 10
  suffix: "_suffix"
  key: "autocomplete"
  score-key: "autocomplete:score"